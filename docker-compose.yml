version: "3"
services:
  web:
    build: .
    environment:
      - API_KEY=APIKEY
      - NODE_ENV=production
    ports:
      - "8002:80"
    volumes:
      - ./db.json:/usr/src/app/db.json
      - ./LiveShoppingList.apk:/usr/src/app/LiveShoppingList.apk

